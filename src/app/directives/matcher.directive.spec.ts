import { MatcherDirective } from './matcher.directive';

describe('MatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new MatcherDirective();
    expect(directive).toBeTruthy();
  });
});
